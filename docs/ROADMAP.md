# 91YOYO App 现状与路线图

版本：v0.2
盘点日期：2026-09-20

## 1. 当前进度

| 模块 | UI | 真实数据/能力 | 当前结论 |
| --- | --- | --- | --- |
| 导航与主题 | 已有 | 本地可运行 | 首页、探索、发布、装备、我的、详情和全屏视频路由已接通 |
| 首页 Feed | 已有 | 本地确定性 Mock | `推荐`/`关注` 双流、可解释排序、分页、刷新、空态和错误态已接通 |
| 本地互动状态 | 已有 | AsyncStorage | 帖子点赞/收藏、评论发布/点赞和关注在统一 Social Store 中同步并持久化 |
| 帖子详情/评论 | 已有 | 统一 Social Store | 详情复用统一媒体组件；一级 Mock 评论、发布、计数和本地持久化已接通 |
| 探索/搜索 | 已有 | 统一 Social Store | 作者、正文、标签搜索，分类、热门话题和媒体发现网格已接通 |
| 发帖 | 已有 | 占位 | 没有系统选取、上传、持久化和重复提交保护 |
| 视频/音频 | 全屏视频流已完成 | 当前视频素材为 `reserved` | 视频封面、上下分页和互动已接通；真实播放、慢放、镜像和音频播放待许可素材加入后完成 |
| 装备 | 已有 | Mock | 没有详情、发布、筛选 API 和状态流转 |
| 个人主页 | 已有 | Mock | 设置项大多为提示框，没有真实编辑和隐私保存 |
| 私聊 | 占位 | 未实现 | 路由存在，计划在 Phase 5 完成 |
| Jam | 占位 | 未实现 | 路由存在，没有地图和位置服务 |
| 后端 | 有历史基础 | 当前阶段停用 | App 前端验收前不继续登录、数据库、COS、正式 API 或部署接入 |
| 工程质量 | 基础 | 已有自动化 | TypeScript 严格模式；Vitest 覆盖 fixtures、排序、探索、视频、评论、Repository、持久化 Store 和现有服务 |
| 原生工程 | iOS 可本地构建 | Android 未验证 | iPhone 17 Pro 模拟器可运行；正式名称与 Bundle Identifier 仍待发布阶段确认 |

结论：当前是可离线运行的前端交互原型，不是可发布的 MVP。先完成全部前端页面和 Mock 功能验收，再开始“登录 → 云端 Feed → COS 上传 → 发布 → App/小程序数据互通”的真实链路。

## 2. 阶段规划

工期取决于人员和后端资源，下面以可验收出口排序，不承诺固定日期。

### Phase 0：前端体验基线（进行中）

- 首页使用确定性的本地推荐/关注双流，状态在页面间共享。
- 探索按分类、搜索和相关性发现内容，不复制首页推荐流。
- 视频进入 TikTok 式全屏上下滑播放；本地授权短视频可真实播放，图片采用 X 式最多四格预览并支持全屏浏览。
- 装备定位为个人悠悠球藏品馆；发布、个人主页和设置使用统一 Mock 数据。
- 前端验收期间不依赖 API URL、SSH 隧道、数据库或 COS。

出口：首页、探索、视频、装备、发布、详情和个人主页的主要流程在 iOS/Android 模拟器可重复验收，本地数据和互动状态一致。

### Phase 1：工程和后端基线

- 固化正式应用标识、环境变量、开发/预发布/生产环境。
- 建立 API migrations、seed、配置校验和数据库权限测试。
- 建立 App service/repository 层，页面不直接依赖数据库或 COS。
- 补 lint、单测、CI 和错误监控基础。

出口：新环境可按文档初始化；未配置密钥时应用给出明确错误；CI 能阻止类型和测试失败的变更。

### Phase 2：账号与只读 Feed

- 手机验证码登录、会话恢复、退出和基础资料。
- 公开 Feed 游标分页、帖子详情、用户主页。
- 缓存上次成功 Feed，提供加载、空、错误和重试状态。

出口：两个测试账号可跨设备登录并看到一致的服务端数据。

### Phase 3：媒体上传与练习播放器

- 图片、视频、音频选择与客户端校验。
- 临时上传、进度、取消、重试、媒体确认与孤儿文件清理。
- 视频倍速/镜像/循环；音频后台播放和锁屏控制；图片预览。
- 创建、删除自己的帖子和可见性设置。

出口：iOS/Android 真机连续完成 20 次不同媒体发布，不出现重复帖子或无法清理的失败上传。

### Phase 4：社区闭环（App MVP）

- 点赞、收藏、一级评论、关注和拉黑。
- 分类/花式筛选，用户、内容和话题搜索。
- 接入只读装备列表和个人装备柜，复用现有界面但不开放卖家联系。
- 举报入口、基础审核状态、账号注销流程。
- 性能、可访问性、弱网和崩溃治理。

出口：满足 PRD 的 App MVP 验收标准，并产出 TestFlight/Android 内测包。

### Phase 5：装备与私聊（v1.1）

- 装备详情、发布、编辑、预留、下架和成交标记。
- 一对一会话、文本/图片/装备卡片消息、未读数和推送。
- 内容和私聊风控、拉黑后会话限制。

出口：买卖双方可完成“浏览 → 联系 → 线下自行成交 → 标记售出”，不涉及平台资金托管。

### Phase 6：Jam 与正式发布（v1.2）

- 地图浏览、活动发布、报名/取消报名、日历和通知。
- 隐私权限文案、商店素材、发布签名、监控和运营后台最小能力。

出口：通过应用商店审核，位置权限仅在进入地图或选择地点时申请。

### Phase 7：微信小程序接入

- 小程序接入相同手机号账号和 `profileId`。
- 按 API 契约替换硬编码 Store，优先接 Feed、详情、互动和个人资料。
- 适配微信上传、播放、订阅消息和网络域名白名单。
- 验证 App 创建、小程序读取；小程序互动、App 实时/刷新后读取。

出口：核心契约测试在 App adapter 与 Uni-app adapter 上同时通过，不存在端专属字段写入共享表。

### Phase 8：国际化与内容翻译

- 接入统一文案键、系统语言检测和用户语言设置。
- 为帖子提供按需自动翻译、原文切换和翻译失败回退。
- 当前阶段只维护中文 Mock，不提前混入英文界面文案或伪翻译结果。

出口：至少中文、英文界面可切换；自动翻译不覆盖原文，App 与小程序共享语言偏好和翻译状态契约。

## 3. 优先级规则

1. 先保证账号、数据权限和写入幂等，再扩充页面。
2. 先完成一条真实纵向链路，再并行开发装备、私聊和 Jam。
3. 影响 App/小程序共享字段的改动必须先更新 API 契约。
4. 支付、担保和结算是独立项目，不能作为装备模块中的一个普通按钮加入。

## 4. 近期交付顺序

近期按前端验收顺序交付：探索内容化 → 全屏视频流 → 详情与评论 Mock → 装备藏品馆 → 发布闭环 → 个人主页与设置。全部通过后再按腾讯云短信认证 → token 会话 → PostgreSQL/COS → 发布同步 → 小程序 adapter 的顺序接入后端。

## 5. 主要风险

| 风险 | 影响 | 处理原则 |
| --- | --- | --- |
| 腾讯云短信签名和模板未配置 | 登录链路无法上线 | Phase 2 完成签名、模板、限流和测试号码验证 |
| 视频转码与内容审核耗时 | 上传成功但长时间不可发布 | 使用明确 processing 状态、异步任务和失败重试，不让客户端假成功 |
| App 标识仍为默认值 | 签名、推送和商店记录后续难迁移 | 生成正式内测包前确认永久 Bundle ID 和 Android package |
| 社区内容与二手交易合规 | 商店审核、投诉和运营风险 | MVP 先建立举报、审核和注销；资金交易作为独立立项 |
| App 与小程序字段实现分叉 | 后续接入受阻 | 契约基于 HTTPS/JSON，两端 adapter 共用版本化 fixtures |
