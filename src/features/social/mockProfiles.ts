import type { ViewerProfile } from './types';

export const currentViewer: ViewerProfile = {
  userId: 'user-lin',
  interestStyles: ['1A', '5A'],
  interestCategories: ['tutorial', 'music', 'contest'],
  followedUserIds: ['user-chen', 'user-xiaoyu', 'user-mori', 'user-zhou'],
};
